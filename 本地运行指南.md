# Gorgon · 本地最小 MVP 运行指南

> 目标:在自己电脑上打开、能改数据、能验证「信息真实性 + 路线导航」这套流程。
> 这一版是**高保真原型**:界面、交互、导航深链接都是真的,**活动数据是手工填的样例**
> (`ui_kits/app/data.js`),还没有自动爬取。验证逻辑先靠你手动填真实活动来跑通。

---

## 它现在能做什么 / 不能做什么

| 能 ✅ | 还不能 ❌(需后续后端) |
|---|---|
| 本地浏览器打开,完整点按体验 | 自动从全网爬取活动 |
| 改 `data.js` 填**真实活动**,立刻在界面看到 | 实时同步 / 账号登录 |
| 信任等级、来源、报错、名额等真实呈现 | 服务器存储、推送通知 |
| 点「导航」深链接到高德/百度/腾讯/Apple 地图 | 坐标系自动转换(见下方注意) |

**所以「验证数据真实性」现在的做法**:你把几条**真实**的周末活动按格式填进 `data.js`,
本地打开看每条的「来源 / 认证等级 / 更新时间 / 地址 / 导航」是否都站得住 —— 跑通这套
人工校验流程,就是 MVP 要验证的核心假设。后面再用后端把「爬取 + 自动核验」自动化。

---

## 两种本地打开方式

### 方式 A — 双击即开(最快,只看效果)
1. 下载 **`deploy/index.html`** 这一个文件(约 8MB,所有代码已内联)。
2. 双击,用 **Chrome / Edge / Safari** 打开即可。
   > 首次需联网(React、图标库走 CDN);之后界面就是完整的 Gorgon App。
   > 缺点:这是打包好的,**不能直接改数据**。

### 方式 B — 本地服务器(推荐,可改真实数据验证)
要改 `data.js` 填真实活动,必须用一个本地服务器(直接双击 `ui_kits/app/index.html`
会因浏览器安全限制加载失败)。任选一种:

**用 Python(电脑大多自带):**
```bash
# 1. 下载整个项目文件夹,进入它
cd 你的项目文件夹
# 2. 起一个本地服务器
python3 -m http.server 8000
# 3. 浏览器打开
#    http://localhost:8000/ui_kits/app/
```

**用 Node(装了 Node 的话):**
```bash
cd 你的项目文件夹
npx serve .
# 按提示打开它给的 http://localhost:xxxx/ui_kits/app/
```

打开后,用编辑器改 `ui_kits/app/data.js` → 刷新浏览器即可看到新数据。

---

## 怎么填一条真实活动(在 `data.js` 的 `activities` 数组里)

```js
{
  id: "r1",                         // 唯一 id
  title: "复旦周末观鸟团",            // 活动名
  category: "outdoor",              // sport/music/art/food/outdoor/study
  date: "周六 6.21", day: "sat", time: "07:00", end: "09:00",
  location: "上海·杨浦", venue: "共青森林公园南门", distance: "1.5km",
  address: "上海市杨浦区军工路 2000 号 共青森林公园南门",
  coord: { lng: 121.545, lat: 31.310 },   // 经纬度(导航用)
  price: "免费", going: 20, capacity: 30, spotsLeft: 10,
  tags: ["观鸟", "新手友好"],

  // —— 真实性字段(MVP 要验证的核心)——
  trust: "verified",                // verified/official/aggregated/unverified
  source: "复旦自然爱好者协会公众号",   // 来源(可溯)
  updated: "2 小时前",
  host: "复旦自然协会", contact: "公众号「复旦自然」",

  desc: "清晨在共青森林公园认鸟,协会提供望远镜。",
  transit: [
    { mode: "bus", line: "公交 8 路", detail: "共青森林公园站下", time: "20 分钟", cost: "¥2", recommended: true },
    { mode: "bike", line: "共享单车", detail: "沿军工路骑行", time: "9 分钟" }
  ],
  bring: ["望远镜（可借）", "饮用水"],
  notes: "雨天取消,以公众号通知为准。",
  refund: "免费活动,无需退款。"
}
```

> 想让一条显示为「待核实」就把 `trust` 设成 `"unverified"`,界面会自动变成琥珀色提示
> 并提醒用户「先核实再前往」—— 这正是要验证的诚实呈现逻辑。

---

## ⚠️ 导航坐标注意(上线前必处理)
各地图商坐标系不同:**高德/腾讯 = GCJ-02,百度 = BD-09,Apple/GPS = WGS-84**。
原型直接传了原始坐标,正式版要按目标地图转换,否则定位会偏 50–700 米。

---

## 之后的路:从本地 MVP → 微信小程序
1. **本地 MVP(现在)** — 验证「人工填真实活动 + 信任呈现 + 导航深链」是否成立。
2. **加后端** — 用爬虫/接口聚合活动,自动给来源打认证等级,落数据库。
3. **微信小程序** — 这套 React 界面**不能直接搬进小程序**(小程序用 WXML/WXSS),
   但**设计系统(颜色/字体/组件规格/交互)可 1:1 复用**。可用 Taro / uni-app
   把 React 思路转成小程序,或按本设计系统重写 UI。需要时我可以帮你出一份
   「设计系统 → 小程序」的映射规范。

---

*本文件随 `ui_kits/app/index.html` 自动打包。改源码后需重新打包 `deploy/index.html`,
不要直接手改打包文件。*
