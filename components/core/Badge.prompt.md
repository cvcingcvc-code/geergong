Count or status badge; or a bare notification dot.

```jsx
<Badge tone="danger">3</Badge>
<Badge tone="mint" dot />
```

Tones: `brand` `mint` `danger` `amber` `neutral`. Pass `dot` for a notification dot with no label.
