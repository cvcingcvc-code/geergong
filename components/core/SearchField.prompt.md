Pill-shaped search field with Lucide search icon — Gorgon's primary 发现 entry point.

```jsx
<SearchField placeholder="搜索活动、地点、标签" size="lg" />
```

Requires Lucide on the page (`lucide.createIcons()` after render). Sizes `md` `lg`.
