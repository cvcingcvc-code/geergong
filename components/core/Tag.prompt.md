Pill tag / chip — activity metadata, filters, status labels (本周末, 免费, 已满).

```jsx
<Tag tone="brand">本周末</Tag>
<Tag tone="mint" dotColor="var(--cat-outdoor)">户外</Tag>
```

Tones: `neutral` `brand` `mint` `warning` `danger` `solid` `ink`. Sizes `sm` `md`. Add `dotColor` for a category dot.
