User avatar — image or initials fallback on a brand-tinted chip.

```jsx
<Avatar name="小林" size="md" />
<Avatar src="/u.jpg" name="Lin" ring />
```

Sizes: `xs sm md lg xl`. Pass `ring` for a brand halo (e.g. own avatar / active state).
