Labeled text input with indigo focus ring + optional leading icon and hint/error.

```jsx
<Input label="昵称" placeholder="你的名字" />
<Input label="邮箱" invalid hint="格式不正确" />
```

Props: `label`, `hint`, `leadingIcon`, `invalid`. Forwards all native input attributes.
